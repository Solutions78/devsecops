# Make backend a Python package so test modules can import using
# `import backend.<submodule>`.

# It is intentionally left minimal; all actual code resides in
# sub-packages such as `backend.orchestrator`.

